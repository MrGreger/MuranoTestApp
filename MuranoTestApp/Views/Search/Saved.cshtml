@model SearchDTO
@{
    ViewData["Title"] = "Saved search";

}


<div class="row">
    <div class="nav-btn"><a asp-action="Index" asp-controller="Search">Search in web</a></div>
    <div class="nav-btn"><b>Search in saved</b></div>
</div>
<form class="searchbar" asp-controller="Search" asp-action="Saved" method="post">
    <input type="text" asp-for="Query" />
    <input type="submit" value="Go">
</form>
@if (Model != null)
{
    @if (Model.Results != null && Model.Results.Any())
    {
        <div class="results">
            @foreach (var item in Model.Results)
            {
                <div class="result">
                    <a class="title" href="@item.URL" target="_blank">@item.Title</a>
                </div>
            }
        </div>
    }
    else
    {
        <div class="results">
            <div>
                No results.
            </div>
        </div>
    }
}

